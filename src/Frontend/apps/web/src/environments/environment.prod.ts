export const environment = {
  production: true,
  apiUrl: '/api/v1',
  // Gerado automaticamente por scripts/generate-version.js (hook "pre*" do npm) a partir de
  // `git rev-list --count HEAD` — não editar à mão, o próximo build sobrescreve.
  appVersion: 'v125',
};
