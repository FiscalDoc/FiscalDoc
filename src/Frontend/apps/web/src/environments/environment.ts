export const environment = {
  production: false,
  apiUrl: 'http://localhost:5000/api/v1',
  // Bump manual a cada leva de funcionalidades entregues (não é o total de commits do git em
  // tempo real) — também usado como chave do modal de novidades, pra saber se o usuário já viu
  // a versão atual.
  appVersion: 'v1.23',
};
