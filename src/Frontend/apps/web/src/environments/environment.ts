export const environment = {
  production: false,
  apiUrl: 'http://localhost:5000/api/v1',
  // Gerado automaticamente por scripts/generate-version.js (hook "pre*" do npm) a partir de
  // `git rev-list --count HEAD` — não editar à mão, o próximo build/serve sobrescreve.
  appVersion: 'v125',
};
