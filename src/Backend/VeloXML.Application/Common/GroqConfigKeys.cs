namespace VeloXML.Application.Common;

// Mesmo padrão de FocusNfeConfigKeys — chave única da plataforma (não por Cliente), configurada
// pelo Administrador na tela de Configurações, guardada na tabela Configuracao.
public static class GroqConfigKeys
{
    public const string ApiKey = "groq.api_key";
}
